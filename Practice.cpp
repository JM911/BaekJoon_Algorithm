#include <bits/stdc++.h>
using namespace std;

int n, m;
int a[100005];

int BNSearch(int target, int st, int en)
{
}

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    cin >> m;
}