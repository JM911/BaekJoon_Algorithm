#include <bits/stdc++.h>
using namespace std;

int N;
int A[1010];

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    cin >> N;

    for (int i = 0; i < N; i++)
    {
        cin >> A[i];
    }


}